# Ochiq portlar bo'yicha avtomatik kollektor topish — O'CHIRILGAN.
# Sabab `sd/docker.conf` dagi bilan bir xil: kuzatiladigan xizmatlar `go.d/*.conf`
# da aniq sanab o'tilgan, taxmin qilish shart emas.

disabled: yes
