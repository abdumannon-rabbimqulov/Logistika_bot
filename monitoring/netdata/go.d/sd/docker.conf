# Netdata'ning avtomatik "service discovery" mexanizmi — O'CHIRILGAN.
#
# NEGA: u ochiq portlarni ko'rib, kollektorlarni O'ZI ulashga urinadi va bizning
# aniq sozlangan job'larimiz USTIGA ikkinchi nusxa qo'shadi. Natijada:
#   • har bir grafik ikki marta (`postgres_logistika_db` va `docker_postgres_db`)
#   • Postgres'ga taxminiy parol bilan ulanishga urinib, logni xatolar bilan to'ldiradi:
#     `password authentication failed for user "netdata"`
#   • alertlar ikki marta ishlaydi
#
# Bizga bu kerak emas: qaysi xizmatni kuzatishni `go.d/*.conf` da o'zimiz aniq
# yozganmiz (postgres, redis, rabbitmq, docker, httpcheck).

disabled: yes
